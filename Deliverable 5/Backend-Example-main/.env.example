# Copy this file to `.env` and fill in values before running the app
PORT=3000
# Example local MongoDB (replace with your Atlas connection string for production)
MONGODB_URI=mongodb://localhost:27017/backend-example
